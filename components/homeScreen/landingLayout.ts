/** Horizontal inset applied to every landing section’s inner container. */
export const SECTION_INLINE = "px-5 sm:px-8 md:px-10 lg:px-14";

/** Full-width content column (max ~1152px + side padding). */
export const LANDING_WIDE = `mx-auto w-full max-w-6xl ${SECTION_INLINE}`;

/** Narrow column for hero copy (max ~896px + same side padding). */
export const LANDING_NARROW = `mx-auto w-full max-w-4xl ${SECTION_INLINE}`;
